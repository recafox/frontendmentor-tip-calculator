
const initialState = {
  tipAmountPerPerson: 0,
  totalPerPerson: 0,
}

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default reducer;